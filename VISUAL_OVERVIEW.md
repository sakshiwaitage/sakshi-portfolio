# 🎨 Resume Backend - Visual Setup Overview

```
YOUR PORTFOLIO STRUCTURE
════════════════════════════════════════════════════════════════════

sakshi-portfolio/
│
├── 📁 src/                          ← FRONTEND
│   ├── 📁 sections/
│   │   └── 📝 hero-section.tsx      ✅ UPDATED - Resume button functional
│   ├── 📁 utils/
│   │   └── 📄 api.ts               ✅ NEW - API utilities
│   ├── 📄 App.tsx
│   ├── 📄 main.tsx
│   └── ...
│
├── 📁 backend/                      ← BACKEND (NEW)
│   ├── 📄 server.js                ✅ NEW - Express server
│   ├── 📄 package.json             ✅ NEW - Backend dependencies
│   ├── 📄 .env                     ✅ NEW - Environment config
│   ├── 📄 .env.example             ✅ NEW - Example env
│   ├── 📄 .gitignore               ✅ NEW - Git ignore
│   ├── 📄 README.md                ✅ NEW - Backend docs
│   └── 📁 public/
│       └── 📁 resume/
│           ├── 📄 README.md        ✅ NEW - Instructions
│           └── 📄 resume.pdf       ⏳ TODO - ADD YOUR RESUME
│
├── 📄 package.json                 ← FRONTEND CONFIG
├── 📄 vite.config.ts
├── 📄 tsconfig.json
├── 📄 eslint.config.js
├── 📄 index.html
│
├── 📚 DOCUMENTATION (NEW)           ← SETUP GUIDES
│   ├── 📖 QUICK_START.md           ⭐ START HERE
│   ├── 📖 SETUP_GUIDE.md
│   ├── 📖 ARCHITECTURE.md
│   ├── 📖 RESUME_BACKEND_README.md
│   ├── 📖 SETUP_COMPLETE.md
│   └── 📖 backend/README.md
│
└── ...other files...
```

---

## 🔄 Data Flow Diagram

```
┌─────────────────────┐
│   VISITOR BROWSER   │
│ http://localhost... │
└──────────┬──────────┘
           │
           │ User clicks "Resume" button
           ▼
┌─────────────────────────────────────┐
│     FRONTEND (React/Vite)           │
│  src/sections/hero-section.tsx      │
├─────────────────────────────────────┤
│ onClick → handleResumeClick()        │
│  ↓                                   │
│ await downloadResume()              │
│  (from src/utils/api.ts)            │
└──────────┬──────────────────────────┘
           │
           │ HTTP GET Request
           │ http://localhost:5000/api/resume
           ▼
┌─────────────────────────────────────┐
│  BACKEND (Express.js)               │
│  backend/server.js                  │
├─────────────────────────────────────┤
│ GET /api/resume                     │
│  ↓                                   │
│ Read backend/public/resume/          │
│ Serve resume.pdf as blob            │
└──────────┬──────────────────────────┘
           │
           │ HTTP Response
           │ Binary PDF Data
           ▼
┌─────────────────────────────────────┐
│  FRONTEND (Browser)                 │
│  utils/api.ts - downloadResume()    │
├─────────────────────────────────────┤
│ Convert blob to File                │
│ Create download link                │
│ Trigger browser download            │
└──────────┬──────────────────────────┘
           │
           │ File download
           │ Sakshi_Waitage_Resume.pdf
           ▼
┌─────────────────────────────────────┐
│    USER'S DOWNLOADS FOLDER          │
│  Sakshi_Waitage_Resume.pdf ✅       │
└─────────────────────────────────────┘
```

---

## 📊 What Was Installed

```
BACKEND DEPENDENCIES (backend/package.json)
═══════════════════════════════════════════════════════════

npm install (installs these packages)

├─ express@^4.18.2
│  └─ Fast, unopinionated web framework for Node.js
│     ├─ Handles HTTP requests/responses
│     ├─ Routing support
│     ├─ Middleware support
│     └─ Static file serving
│
├─ cors@^2.8.5
│  └─ Cross-Origin Resource Sharing middleware
│     ├─ Allows requests from frontend
│     ├─ Configurable origins
│     ├─ Handles preflight requests
│     └─ Security headers
│
└─ dotenv@^16.3.1
   └─ Environment variable management
      ├─ Load from .env files
      ├─ Configuration management
      ├─ Port configuration
      └─ Environment switching
```

---

## 🚀 Startup Process

```
STEP 1: TERMINAL 1 - START BACKEND
═════════════════════════════════════

$ cd backend
$ npm install          ← Download packages (once only)
$ npm run dev          ← Start Express server

✅ Backend server is running on http://localhost:5000
📄 Resume endpoint: http://localhost:5000/api/resume
👀 View resume: http://localhost:5000/api/resume/view

   ▲
   │
Waiting for requests...
   │


STEP 2: TERMINAL 2 - START FRONTEND
═════════════════════════════════════

$ npm run dev          ← Start Vite dev server

➜  Local:   http://localhost:5173/

   ▲
   │
Ready to browse...
   │


STEP 3: BROWSER - TEST THE FEATURE
═════════════════════════════════════

1. Open http://localhost:5173/
   ↓
2. Scroll to top (hero section)
   ↓
3. Click "Resume" button
   ↓
4. File downloads! 🎉
```

---

## 🔌 API Endpoints

```
HEALTH CHECK
─────────────────────────────────────
┌─────────────────────┐
│ GET /api/health     │
├─────────────────────┤
│ Purpose:            │
│ Check if backend    │
│ is running          │
├─────────────────────┤
│ Response:           │
│ {                   │
│   "status": "ok"    │
│ }                   │
├─────────────────────┤
│ Status Code:        │
│ 200 OK              │
└─────────────────────┘


DOWNLOAD RESUME
─────────────────────────────────────
┌──────────────────────┐
│ GET /api/resume      │
├──────────────────────┤
│ Purpose:             │
│ Download resume PDF  │
│ as file              │
├──────────────────────┤
│ Response:            │
│ Binary PDF Data      │
│ Content-Type: app/  │
│   pdf                │
├──────────────────────┤
│ Status Code:         │
│ 200 OK               │
│ 404 Not Found        │
│ 500 Server Error     │
└──────────────────────┘


VIEW RESUME
─────────────────────────────────────
┌──────────────────────────┐
│ GET /api/resume/view     │
├──────────────────────────┤
│ Purpose:                 │
│ Open resume in new tab   │
│ (browser view)           │
├──────────────────────────┤
│ Response:                │
│ Binary PDF Data          │
│ (inline display)         │
├──────────────────────────┤
│ Status Code:             │
│ 200 OK                   │
│ 404 Not Found            │
│ 500 Server Error         │
└──────────────────────────┘
```

---

## 🎯 Component Interaction

```
hero-section.tsx (FRONTEND)
═══════════════════════════════════════════════════════

┌─────────────────────────────────────────────┐
│ Import Statement                            │
│ import { downloadResume } from api.tsx      │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ State Management                            │
│ const [isDownloading, setIsDownloading]     │
│          = useState(false)                  │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Button Element                              │
│ <button onClick={handleResumeClick}>        │
│   {isDownloading ? "Downloading..." : "..."} │
│ </button>                                   │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Handler Function                            │
│ const handleResumeClick = async () => {     │
│   setIsDownloading(true)                    │
│   try {                                     │
│     await downloadResume()                  │
│   } catch (error) {                         │
│     alert(error message)                    │
│   } finally {                               │
│     setIsDownloading(false)                 │
│   }                                         │
│ }                                           │
└─────────────────────────────────────────────┘


api.ts (UTILITIES)
═══════════════════════════════════════════════════════

┌─────────────────────────────────────────────┐
│ API_BASE_URL Configuration                  │
│ "http://localhost:5000" (or env variable)   │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ downloadResume() Function                   │
│ └─ Fetches from API_BASE_URL/api/resume     │
│ └─ Gets blob data                           │
│ └─ Creates download link                    │
│ └─ Triggers browser download                │
│ └─ Cleans up resources                      │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Browser Download Manager                    │
│ Saves file: Sakshi_Waitage_Resume.pdf       │
└─────────────────────────────────────────────┘


server.js (BACKEND)
═══════════════════════════════════════════════════════

┌─────────────────────────────────────────────┐
│ Express Server Setup                        │
│ app.listen(5000)                            │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Middleware Configuration                    │
│ ├─ CORS enabled                             │
│ ├─ JSON parser                              │
│ └─ Static file serving                      │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ Route Handlers                              │
│ ├─ GET /api/health                          │
│ ├─ GET /api/resume  ← Resume download       │
│ └─ GET /api/resume/view                     │
└─────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│ File System                                 │
│ backend/public/resume/resume.pdf            │
└─────────────────────────────────────────────┘
```

---

## 📈 Request/Response Cycle

```
Timeline: User clicks Resume button → File downloads
───────────────────────────────────────────────────────────

0ms    │ User clicks Resume button
       │
1ms    │ ┌─────────────────────────────────────┐
       │ │ handleResumeClick() executes        │
       │ │ setIsDownloading(true)              │
       │ │ Button shows "Downloading..."       │
       │ └─────────────────────────────────────┘
       │
10ms   │ ┌─────────────────────────────────────┐
       │ │ downloadResume() called             │
       │ │ fetch("http://localhost:5000/...")  │
       │ │ Sends HTTP GET request              │
       │ └─────────────────────────────────────┘
       │
15ms   │ ┌─────────────────────────────────────┐
       │ │ NETWORK DELAY                       │
       │ │ Request travels to backend          │
       │ └─────────────────────────────────────┘
       │
25ms   │ ┌─────────────────────────────────────┐
       │ │ Backend receives request            │
       │ │ Checks if resume.pdf exists         │
       │ │ Reads file from disk                │
       │ │ Prepares response                   │
       │ └─────────────────────────────────────┘
       │
30ms   │ ┌─────────────────────────────────────┐
       │ │ Backend responds with PDF blob      │
       │ │ Sends as Content-Type: pdf          │
       │ │ Includes filename in headers        │
       │ └─────────────────────────────────────┘
       │
35ms   │ ┌─────────────────────────────────────┐
       │ │ NETWORK DELAY                       │
       │ │ Response travels to frontend        │
       │ └─────────────────────────────────────┘
       │
45ms   │ ┌─────────────────────────────────────┐
       │ │ Frontend receives blob              │
       │ │ Converts to File object             │
       │ │ Creates download link               │
       │ └─────────────────────────────────────┘
       │
50ms   │ ┌─────────────────────────────────────┐
       │ │ Browser download initiated          │
       │ │ Saves to Downloads folder           │
       │ │ setIsDownloading(false)             │
       │ │ Button returns to "Resume"          │
       │ └─────────────────────────────────────┘
       │
100ms  │ ✅ FILE DOWNLOAD COMPLETE
       │ Sakshi_Waitage_Resume.pdf saved
       │

Total Time: ~100ms (plus actual file transfer ~1-2 seconds)
```

---

## ✅ Verification Checklist

```
SETUP VERIFICATION
═════════════════════════════════════════

Files Created:
  ✅ backend/server.js
  ✅ backend/package.json
  ✅ backend/.env
  ✅ src/utils/api.ts
  ✅ Documentation files (5 files)

Files Updated:
  ✅ src/sections/hero-section.tsx

Backend Status:
  ⏳ Run: cd backend && npm install
  ⏳ Run: npm run dev
  ? Should show: ✅ Backend running on :5000

Frontend Status:
  ✅ Run: npm run dev
  ? Should show: ➜  Local: http://localhost:5173/

Resume File:
  ⏳ TODO: Add resume.pdf to backend/public/resume/
  ? Check: File named exactly "resume.pdf"
  ? Path: backend/public/resume/resume.pdf

Feature Test:
  ⏳ Open: http://localhost:5173/
  ⏳ Click: "Resume" button
  ? Should: Download Sakshi_Waitage_Resume.pdf
  ? Check: No console errors
```

---

## 🎓 Learning Resources

This setup teaches:

- ✅ Express.js basics
- ✅ CORS configuration
- ✅ Static file serving
- ✅ REST API design
- ✅ Frontend-Backend communication
- ✅ File downloads in browsers
- ✅ Error handling patterns
- ✅ Environment management

---

## 🚀 You're Ready!

Everything is set up and documented.

**Next action:** Add your `resume.pdf` file! 📄

Then follow [QUICK_START.md](QUICK_START.md) for the 3-minute setup.
